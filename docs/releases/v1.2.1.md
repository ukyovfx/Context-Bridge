# Context Bridge v1.2.1

- Fixed the same-workspace branch-change false identity mismatch.
- Branch and detached state remain fresh Git-state evidence.
- Durable repository and workspace mismatch checks remain fail-closed.
- `integrate` now requires explicit confirmation for file writes, including on already-registered workspaces.
- No schema migration is required.
